export default function SingleplayerLayout({children}) {
    return (
        <section className="flex flex-grow bg-cb-homepage bg-cover bg-center bg-clip-border bg-fixed justify-center items-center">
            {children}
        </section>
    )
}