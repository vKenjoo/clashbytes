export default function MultiplayerLayout({children}) {
    return (
        <section className="flex flex-grow bg-cb-homepage bg-cover bg-center bg-clip-border bg-fixed justify-center">
            {children}
        </section>
    )
}